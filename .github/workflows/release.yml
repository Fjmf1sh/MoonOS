name: Release Moon OS update

# Publishes a Moon Shell self-update to GitHub Releases so flashed consoles can
# pull it (System settings -> Update). Runs entirely in the cloud — nothing to
# run locally. Trigger it two ways:
#   * GitHub website: Actions tab -> "Release Moon OS update" -> Run workflow
#   * or push a git tag like v2026.07.10
#
# It compiles ONLY the moon-shell binary (aarch64, Debian bookworm — the Pi's
# runtime), which is far lighter than building the whole image.

on:
  # Manual button in the Actions tab.
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish (blank = today's date + run number)"
        required: false
      prerelease:
        description: "Mark as a pre-release"
        type: boolean
        default: false
  # Auto-publish whenever the shell/UI or fork changes land on main.
  # (Add [skip ci] to a commit message to suppress a release.)
  push:
    branches:
      - main
    paths:
      - 'moon-shell/**'
      - 'third_party/moonlight-qt'
    tags:
      - 'v*'

permissions:
  contents: write

# Never let two releases build at once — they'd fight over tags/assets.
concurrency:
  group: moonos-release
  cancel-in-progress: false

jobs:
  build-and-release:
    # Build on GitHub's native arm64 runner, then run the compiler inside a
    # Debian bookworm container. This preserves the Pi/bookworm runtime ABI
    # without QEMU user-mode emulation, which has failed during apt package
    # configuration before the source build even starts.
    runs-on: ubuntu-24.04-arm
    steps:
      # Deliberately DON'T fetch submodules here: prepare-fork.sh clones
      # moonlight-qt fresh at its pinned public commit, so CI is immune to a
      # stray/private submodule pointer ("not our ref" fetch failures).
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Prepare the Moonlight fork (clone upstream + copy Moon Shell + patch)
        run: bash scripts/prepare-fork.sh

      - name: Work out the version
        id: ver
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            V="${{ github.event.inputs.version }}"
          elif [ "${{ github.ref_type }}" = "tag" ]; then
            V="${GITHUB_REF_NAME#v}"
          else
            # Date plus the run number, so several releases in one day still
            # sort as newer (consoles only update to a higher version).
            V="$(date +%Y.%m.%d).${{ github.run_number }}"
          fi
          echo "version=$V" >> "$GITHUB_OUTPUT"
          echo "tag=v$V" >> "$GITHUB_OUTPUT"
          echo "Publishing version: $V"

      # Qt/FFmpeg dev headers can eat a lot of disk on the default ~14GB free
      # on a GitHub-hosted runner. Reclaim space from toolchains this job
      # never uses before the build.
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL || true
          sudo docker image prune -af || true
          df -h /

      - name: Compile moon-shell (aarch64)
        run: |
          set -ex
          docker run --rm \
            --volume "${GITHUB_WORKSPACE}:/work" \
            --workdir /work \
            debian:bookworm \
            bash -euxo pipefail -c '
            set -ex
            export DEBIAN_FRONTEND=noninteractive
            apt-get update
            apt-get install -y --no-install-recommends \
              build-essential file pkg-config \
              qt6-base-dev qt6-declarative-dev qt6-base-dev-tools libqt6svg6-dev \
              libgl1-mesa-dev libegl1-mesa-dev \
              libopus-dev libsdl2-dev libsdl2-ttf-dev libssl-dev \
              libavcodec-dev libavformat-dev libswscale-dev \
              libva-dev libvdpau-dev libdrm-dev libxkbcommon-dev wayland-protocols \
              libcec-dev

            # qmake probes the compiler (g++ -E -dM ...) to learn its default
            # include/library search paths, then parses the plain-English
            # diagnostic output. A non-C locale can make gcc emit localized
            # or reformatted text there, which breaks that parser with
            # "failed to parse default search paths from compiler output" /
            # "QMAKE_CXX.COMPILER_MACROS is not defined" — seen once before
            # in CI even though the same qmake call succeeds fine inside the
            # pi-gen chroot used by os-image/build.sh.
            export LC_ALL=C LANG=C TERM=dumb GCC_COLORS=
            echo "--- compiler probe diagnostics ---"
            g++ --version
            g++ -dumpmachine
            # Avoid piping directly into head here: with pipefail enabled,
            # head can close early and turn this diagnostic probe into a
            # build-stopping SIGPIPE from g++.
            g++ -E -dM -x c++ /dev/null > /tmp/moonos-compiler-macros.txt
            head -5 /tmp/moonos-compiler-macros.txt
            echo "-----------------------------------"

            cd /work/third_party/moonlight-qt
            mkdir -p build && cd build
            qmake6 ../moonlight-qt.pro
            echo "--- generated makefiles ---"
            find . -maxdepth 2 -name "Makefile*" -print | sort
            echo "---------------------------"
            # Capped (not $(nproc)) because Qt/C++ builds can exceed the
            # runner's memory and get silently OOM-killed with no compiler
            # diagnostic.
            if [ -f Makefile ]; then
              make -j2 release
            elif [ -f Makefile.Release ]; then
              make -f Makefile.Release -j2
            else
              echo "ERROR: qmake did not generate Makefile or Makefile.Release" >&2
              exit 1
            fi
            MOON_SHELL_BIN="$(find app -type f -name moon-shell -perm /111 | sort | head -1)"
            if [ -z "$MOON_SHELL_BIN" ]; then
              echo "ERROR: moon-shell binary was not produced" >&2
              find app -maxdepth 4 -type f -name "moon-shell*" -print >&2 || true
              exit 1
            fi
            mkdir -p /work/deploy/update
            install -m 755 "$MOON_SHELL_BIN" /work/deploy/update/moon-shell-arm64
            echo "Built:"; file "$MOON_SHELL_BIN" || true
            chown -R "$(stat -c %u /work):$(stat -c %g /work)" /work/deploy
            '

      - name: Package release assets
        run: |
          cd deploy/update
          echo "${{ steps.ver.outputs.version }}" > version
          sha256sum moon-shell-arm64 > moon-shell-arm64.sha256
          echo "--- assets ---"; ls -l; echo "--- checksum ---"; cat moon-shell-arm64.sha256
          file moon-shell-arm64 | grep -q aarch64 || { echo "::error::binary is not aarch64"; exit 1; }

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: Moon OS ${{ steps.ver.outputs.version }}
          make_latest: true
          prerelease: ${{ github.event.inputs.prerelease == 'true' }}
          files: |
            deploy/update/version
            deploy/update/moon-shell-arm64
            deploy/update/moon-shell-arm64.sha256
