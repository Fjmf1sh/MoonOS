# Moon OS update configuration. The "Update Moon OS" button runs
# /usr/lib/moonos/update.sh, which upgrades OS packages, pulls the installed
# git checkout recorded in /etc/moonos/install.conf, and reruns install.sh.

# Upgrade Debian / Raspberry Pi OS packages during an update (1 = yes).
DO_APT=1
